import { Meta, Canvas, Controls, ArgTypes } from '@storybook/blocks';
import * as TextStories from './Text.stories';

<Meta title='Components/Text' of={TextStories} />

# Text

## Default

<Canvas of={TextStories.Default} />
<Controls of={TextStories.Default} />
