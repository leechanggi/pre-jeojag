import { Button } from '@components';

const meta = {
	title: 'Components/Button',
	component: Button,
	parameters: {
		layout: 'padded',
	},
};

export default meta;

export const Default = {
	args: {
		variant: 'default',
		size: 'default',
		children: 'button',
	},
};
